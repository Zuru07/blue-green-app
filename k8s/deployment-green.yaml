apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodejs
      env: green
  template:
    metadata:
      labels:
        app: nodejs
        env: green
    spec:
      containers:
      - name: nodejs
        image: zuru07/bluegreen-sample:v1
        ports:
        - containerPort: 3000